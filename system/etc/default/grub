# GRUB boot loader configuration
# Copy to /etc/default/grub and run: sudo grub-mkconfig -o /boot/grub/grub.cfg

GRUB_DEFAULT='0'
GRUB_TIMEOUT='5'
GRUB_DISTRIBUTOR='Arch Linux'
GRUB_CMDLINE_LINUX_DEFAULT='nowatchdog nvme_load=YES nvidia_drm.modeset=1 loglevel=3 quiet splash'
GRUB_CMDLINE_LINUX=""

# Preload both GPT and MBR modules so that they are not missed
GRUB_PRELOAD_MODULES="part_gpt part_msdos"

# Uncomment to enable booting from LUKS encrypted devices
#GRUB_ENABLE_CRYPTODISK=y

# Set to 'countdown' or 'hidden' to change timeout behavior,
# press ESC key to display menu.
GRUB_TIMEOUT_STYLE=menu

# Uncomment to use basic console
GRUB_TERMINAL_INPUT=console

# The resolution used on graphical terminal
GRUB_GFXMODE=auto

# Uncomment to allow the kernel use the same resolution used by grub
GRUB_GFXPAYLOAD_LINUX=keep

# Uncomment to disable generation of recovery mode menu entries
GRUB_DISABLE_RECOVERY='true'

# GRUB Theme - uncomment to use
#GRUB_THEME="/boot/grub/themes/dracula/theme.txt"

# Background image
#GRUB_BACKGROUND='/boot/grub/themes/background.png'

# Uncomment to make GRUB remember the last selection
#GRUB_SAVEDEFAULT=true
#GRUB_DEFAULT=saved

# Uncomment to disable submenus in boot menu
GRUB_DISABLE_SUBMENU='false'

# Enable os-prober for dual boot
GRUB_DISABLE_OS_PROBER=false
